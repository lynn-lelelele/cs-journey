# Mario 🍄 · 2026.07.29

CS50 Problem Set 1：打印马里奥双金字塔。

## 学到

- `for` 循环嵌套：外层控制行数，内层控制空格和 `#`
- `scanf` 读用户输入（C 语言里要写 `&n`）
- 循环条件的设计：`n - i - 1` 个空格，`i + 1` 个 `#`

## 踩坑

- 中文括号 `（）` vs 英文括号 `()` — 最高频报错源
- `printf` 后面漏分号
- 变量名写了 `n` 但前面没声明 `int n;`

## 下次改进

学了数组后可以试试把左右金字塔的循环抽出来。
