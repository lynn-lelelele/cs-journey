# Mario · 2026.07.29

CS50 Problem Set 1：双金字塔。

## 实现

- `for` 循环嵌套：外层控制行，内层控制空格 / `#`
- `scanf` 读取输入
- 循环条件：`n - i - 1`（空格），`i + 1`（`#`）

## 注意

- C 语言区分中文 `（）` 和英文 `()`，编译不通过
- `printf` 末尾需要 `;`
- 变量需先声明后使用

## 计划

- 数组 + 循环重构左右金字塔
